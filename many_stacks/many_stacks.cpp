// many_stacks.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include "stack.h"

int main()
{
	array_s f(3);
	stack a(f), b(f), c(f), d(f);
	system("pause");
    return 0;
}

